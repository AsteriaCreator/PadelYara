{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "build": {
    "commands": [
      "playwright install --with-deps chromium"
    ]
  },
  "deploy": {
    "startCommand": "cd Backend && uvicorn eversports_service:app --host 0.0.0.0 --port $PORT"
  }
}
