{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python -m uvicorn eversports_service:app --host 0.0.0.0 --port ${PORT:-8000}'"
  }
}
