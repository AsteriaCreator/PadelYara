services:
  - type: web
    name: neo-padel-checker-backend
    runtime: python
    buildCommand: pip install -r Backend/requirements.txt && python -m playwright install --with-deps chromium
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT --app-dir Backend
